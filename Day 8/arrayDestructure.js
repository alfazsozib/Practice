const colors = ['red', 'green', 'blue', 'yellow'];

// Basic 
const [first, second] = colors;

// skip elements using comma 
const [, , third] = colors

// Rest elements 
const [head, ...tail] = colors


// swap variables - elements 
